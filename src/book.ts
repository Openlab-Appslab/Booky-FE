export class book {
    id: number;
    title: string;
    author: string;
    description: string;
    price: number;

        constructor(
        title: string,
        author: string,
        description: string,
        price: number,
        ) {}
}