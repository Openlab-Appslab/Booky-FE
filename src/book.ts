export class allBooks {
    title: string;
    author: string;
    description: string;
    price: number;

        constructor(
        title: string,
        author: string,
        description: string,
        price: number,
        ) {}
}